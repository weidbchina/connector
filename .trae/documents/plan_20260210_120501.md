I will fix the two reported issues in `SqlClient.tsx` with the following specific changes:

1. **Strict Autocomplete for Fields**:

   * I will modify `provideCompletionItems` to explicitly check if the character immediately preceding the cursor is a dot (`.`).

   * If a dot is detected, the system will enter "Field Mode". In this mode, it will attempt to find matching columns.

   * **Crucial Fix**: If no columns are found (or the table alias cannot be resolved), it will strictly return an empty suggestion list (`[]`), preventing the fallback to global table names and keywords.

2. **Result Set Display Binding**:

   * I will update the `Table` component's column definition. Specifically, I will change `dataIndex: col` to `dataIndex: [col]` (array format). This ensures that Ant Design correctly treats column names containing dots (which can happen with aliases or expressions) as flat keys rather than nested object paths, resolving potential data binding failures

